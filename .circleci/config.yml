version: 2.1
description: # The purpose of this orb

orbs:
  node:
    executors:
      node:
        docker:
          - image: cimg/python:3.7-node
    commands:
      install:
        steps:
          - run: echo "We will now do special things"
    jobs:
      myjob:
        executor: node
        steps:
          - npm --version
          - node --version

workflows:
  main:
    jobs:
      - my-orb/myjob
